export enum HeroType {
    // None
    None,

    // Range attack:
    Archer,
    Shaman,
    Mage,

    // Melee atack
    Knight,
    Druid,
    Vendigo,
    Guardian,
}

export enum Artifact {
    None,
    WoodenBow,
    ShordSword,
    WoodenShield,
}

export enum DungeonType {
    None,
    GoblinCamp,
    WitchTown,
    BlackTower,
}